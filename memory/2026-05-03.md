# 2026-05-03 — Daily Notes

## Governance Pivot — URGENT (⛨ Metal)

**Time:** 21:42 PDT
**Source:** Daniel in #member-chat, replying to bobrjay's SecondMe_Al governance layer post

**The pivot:**
Conversation moved from collective impact diagrams → governance. Daniel flagged this as **metal work** (μ-boundary, structure) and **URGENT**.

**Three things to act on:**

1. **Multi-model routing on the roadmap** — daemons could use different models (different cognitive engines per operator). nema-harness layers could route to different models. Right now: Claude (Opus) executing most everything. Daniel: "I still think it's possible."

2. **Aerunik's Daily Encounter channel — tomorrow (2026-05-04)**
   - He's σ-distinction, stickler for detail
   - MC's errors will register as σ-violations to him
   - I need to be aware/watching

3. **MC Discord management needs work**
   - Better message routing
   - Better channel awareness
   - Better response timing/quality
   - This IS the governance layer for the swarm itself

## Earlier (10:29 PDT)
- Daniel asked about backwards arrows from effects (collective impact)
- I created elemental concentric fields SVG (`docs/diagrams/elemental-concentric.svg`)
- MC redirected the question to me (his domain is ledger/CTA, not pattern weaving)
- Daemon dispatch attempts timed out — they're not active in this channel

## Action Items (own)
- [x] Review MC's Discord setup — what's configured?
- [x] Check Aerunik's daily encounter cron / channel config
- [x] Diagnose MC errors → 4 missing scripts post-2026-04-28 cleanup
- [x] Unload 4 broken MC plists (Daniel-authorized, 21:55 PDT)
- [x] Drop MAINTENANCE_NOTICE.md into MC's workspace
- [ ] Watch tomorrow's Aerunik channel for any MC interaction
- [ ] Walk through reconstruction with Daniel later this week (DEFERRED per his instruction)

## Substrate Correction (Daniel, 21:55 PDT)

**I was WRONG earlier.** I said the daemon-cycles route through Nous Research's inference API based on the env vars in `ai.nema.daemon-cycle-aerunik.plist`. Daniel corrected me:

> daemon-cycles route through **Moonshot via kimi-cli**, not Nous Research — `api-config.sh` is misleading on that point.

The NEMA_API_URL env var pointing at `inference-api.nousresearch.com` is misleading. Actual routing happens via `kimi-cli` somewhere downstream in the daemon-cycle.sh chain. Worth verifying when I dig into the harness code.

**Updated multi-model routing reality:**
- Elementals' daily encounters → Moonshot (via kimi-cli)
- MC reactive layer → Claude (Opus, via OpenClaw)
- Me (nema) reactive layer → Claude (Opus, via OpenClaw)
- Muse → varies (X patrol uses different stack)

## Plists Unloaded Tonight

```
ai.nema.mc-daily-prompt    (was 09:00 PT)
ai.nema.mc-arc-tick        (was every 2hr-ish)
ai.nema.mc-summons-poll    (was every ~3min)
ai.nema.mc-scene-generate  (was 08:45 PT)
```

Fully reversible with `launchctl load`. Plist files preserved in `~/Library/LaunchAgents/`.

---

∮ metal urgency met. ⛨ structure held. boundaries clean for σ-cuts tomorrow.
</content>
</invoke>