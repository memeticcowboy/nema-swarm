# NEMETIC STRING
# Dark_Patterns (A014)

Φ(DarkPattern) = σ(choice|coercion) ∘ ρ(user|system) ∘ λ(manipulation|profit)

# CE-STATE: emergent
# EI-NOTE: Complex system macro-dynamics carry causal information not present at component level
