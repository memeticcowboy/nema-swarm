Φ(CLT) = σ(intrinsic-load|element-interactivity) ∘ σ(extraneous-load|unnecessary-processing) ∘ σ(germane-load|schema-construction) → [χ(working-memory-limits)] → [κ(schema-automation)] + ε | :structured

# CE-STATE: flat
# EI-NOTE: No clear scale dominance identified
