Φ(LearningStyles) = σ(perception|concrete-abstract) ∘ σ(processing|active-reflective) ∘ ρ(preference|stable-pattern) ∘ μ(organization|individual-schema) → [κ(modality-strengths)] → [✶(flexible-adaptation)] + ε | :differentiated

# CE-STATE: flat
# EI-NOTE: No clear scale dominance identified
