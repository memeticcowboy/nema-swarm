# NEMETIC STRING
# Norm_Persistence (A02E)

Φ(NormPersist) = σ(normal|deviant) ∘ ρ(reinforcement|social) ∘ λ(inertia|institutional) ∘ β(entrenchment|feedback) ∘ δγ(comfort|familiarity) ∘ μ(boundary|policed) + ε | :structuring

# CE-STATE: emergent
# EI-NOTE: Complex system macro-dynamics carry causal information not present at component level
